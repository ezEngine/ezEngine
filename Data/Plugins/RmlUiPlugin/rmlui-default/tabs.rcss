tabset {
	display: block;
}

tabs {
	display: flex;
	/* Flexbox makes tab layout easier */
	flex-direction: row;
	gap: 1dp;
	/* Spacing between tabs */
	position: relative;
	clip: none;
	margin-bottom: 0dp;
	/* Space between tabs and content */
}

tab {
	width: 90dp;
	padding: 0 20dp;
	line-height: 40dp;
	display: inline-block;

	font-size: 15dp;
	color: #ddd;
	text-align: center;

	decorator: tiled-horizontal(tableheader-l, tableheader-c, tableheader-r);
	image-color: #cffc;

	/* Makes tabs reachable with the keyboard. Enter or space on a focused tab clicks it, which selects it. */
	nav: auto;
	tab-index: auto;
}

tab:hover,
tab:focus-visible {
	image-color: #fffe;
	color: #fff;
}

tab:active,
tab:selected {
	image-color: #fff;
	color: #fff;
}